<%- include ('header') %>

    <div class="col-12 mt-5">
        <div class="card form-card">
            <div class="card-body">
                <h4 class="header-title">Edit Privacy Laws</h4>
                <form action="/privacy_laws/edit" method="POST">
                    <% if (locals.error) { %>
                        <div class="alert alert-danger error-msg"><%= error %></div>
                    <% } %>
                    <div class="form-group">
                        <label for="id" class="col-form-label">ID</label>
                        <input class="form-control" type="text" id="id" name="id" value="<%= result[0].law_id %>" readonly required>
                    </div>
                    <div class="form-group">
                        <label for="title" class="col-form-label">Title</label>
                        <input class="form-control" type="text" id="title" name="title" value="<%= result[0].law %>" required>
                    </div>
                    <div class="form-group">
                        <label for="description" class="col-form-label">Description</label>
                        <textarea class="form-control" id="description" name="description" rows="4" required><%= result[0].description %></textarea>
                    </div>
                    <div class="form-group">
                        <label for="link" class="col-form-label">Link</label>
                        <input class="form-control" type="url" id="link" name="link" value="<%= result[0].full_link %>" required>
                    </div>
                    <button type="submit" class="btn btn-primary mt-4 pr-4 pl-4">Submit</button>
                </form>
            </div>
        </div>
    </div>

<%- include ('footer') %>