<%- include ('header') %>

    <!-- Progress Table start -->
    <div class="col-12 mt-5">
        <div class="card">
            <div class="card-body">
                <h4 class="header-title">Data Types Table
                    <a class="btn btn-rounded btn-xs btn-success ml-3" href="/datatypes/add"><i class="fa fa-plus"></i></a>
                </h4>
                <div class="single-table">
                    <div class="table-responsive">
                        <table class="table table-hover progress-table text-center">
                            <thead class="text-uppercase">
                                <tr>
                                    <th scope="col">#</th>
                                    <th scope="col">ID</th>
                                    <th scope="col">Data Type</th>
                                    <th scope="col">action</th>
                                </tr>
                            </thead>
                            <tbody>
                            <% if (result.length > 0) { %>
                                <% for (var i = 0; i < result.length; i++) { %>
                                <tr>
                                    <th scope="row"><%= i+1; %></th>
                                    <td><%= result[i]['data_type_id']; %></td>
                                    <td><%= result[i]['data_type_name']; %></td>
                                    <td>
                                        <ul class="d-flex justify-content-center">
                                            <li><a href="/datatypes/edit?id=<%= result[i]['data_type_id']; %>" class="text-secondary"><i class="fa fa-edit"></i></a></li>
                                            <!-- <% id = result[i]['data_type_id']; %>
                                            <li><a onclick="return confirmDelete('/datatypes/delete?id=<%= id %>');" class="text-danger"><i class="ti-trash"></i></a></li> -->
                                        </ul>
                                    </td>
                                </tr>
                                <% } %>
                            <% } else { %>
                                <tr style="text-align: center; padding: 100px;"><td colspan="4">No results found.</td></tr>
                            <% } %>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- Progress Table end -->

<%- include ('footer') %>